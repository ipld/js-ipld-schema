schema: |
  type UnionKinded union {
    | Foo int
    | Bar bool
    | Baz string
  } representation kinded
  type Foo int
  type Bar bool
  type Baz string
expected:
  {
    "UnionKinded": {
      "kind": "union",
      "representation": {
        "kinded": {
          "int": "Foo",
          "bool": "Bar",
          "string": "Baz"
        }
      }
    },
    "Foo": {
      "kind": "int"
    },
    "Bar": {
      "kind": "bool"
    },
    "Baz": {
      "kind": "string"
    }
  }
blocks:
  - actual:
      100
    expected:
      100
  - actual:
      true
    expected:
      true
  - actual:
      "this here is baz"
    expected:
      "this here is baz"
badBlocks:
  - 100.1
  - { "foo": 100 }
  - { "bar": true }
  - { "baz": "don't match" }
  - { }
  - [ 1, 2, 3 ]
